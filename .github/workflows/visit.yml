name: Trigger AI Summary on Schedule
on:
  schedule:
    # מריץ כל 5 שעות, כמו שהגדרנו קודם
    - cron: '0 */5 * * *'
  workflow_dispatch: # מאפשר הפעלה ידנית

jobs:
  trigger-ai-function:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger AI Summary Function and Show Response
        run: |
          echo "Calling the AI function..."
          curl -X GET https://hanahotplus-jlm.base44.app/functions/triggerAutoAISummary
          echo ""
          echo "Function call completed."
